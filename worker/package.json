{
  "name": "air3-signal-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Air3 Live signaling on Cloudflare Workers + Durable Objects",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run --outdir dist"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
